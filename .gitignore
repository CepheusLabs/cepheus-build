__pycache__/
*.py[cod]
.DS_Store
.venv/
dist/
build/
*.egg-info/

# Local first-party dependency overrides generated by `cepheus-build deps`.
pubspec_overrides.yaml
go.work
go.work.sum

# build run logs (keep the shared build-history.json)
history/*/

# local Claude Code harness state
.claude/

# Locally-generated auto-update Ed25519 private seeds (scripts/cl-update-keygen.sh).
# The private seed must live ONLY in the CI secret CL_UPDATE_ED25519_PRIVATE_KEY;
# never commit anything under keys/.
keys/

# Per-user SSH public key seeded into the Windows VM by oem/install.bat
docker/windows/oem/authorized_keys
