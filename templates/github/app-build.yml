name: app-build

on:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: CepheusLabs/cepheus-build/.github/workflows/app-build.yml@main
    with:
      product: printdeck
      toolkit-ref: main
      runner-profile: github-hosted
      targets: all
